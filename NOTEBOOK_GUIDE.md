# DB 조회 노트북 사용 설명서

> 대상: `hhhs_db_manager.ipynb` · 기준: 2026-09-09, 조회 코드 커밋 `82cc961`
>
> SQL과 Python에 익숙하지 않은 사용자가 입력창으로 테이블을 찾고, 필요한 데이터를 조회해 CSV·Excel로 저장하는 방법입니다. 실제 DB 결과나 접속정보는 수록하지 않습니다. 예시의 날짜·검색값·회사코드는 사용자가 대상 환경에 맞게 확인해야 합니다.

[README](README.md) · [노트북 열기](hhhs_db_manager.ipynb)

## 목차

1. [먼저 알아둘 것](#1-먼저-알아둘-것)
2. [설치와 커널 선택](#2-설치와-커널-선택)
3. [셀과 입력창 조작](#3-셀과-입력창-조작)
4. [처음 한 번 따라 하기](#4-처음-한-번-따라-하기)
5. [0절 접속·권한 확인](#5-0절-접속권한-확인)
6. [1절 테이블 찾기](#6-1절-테이블-찾기)
7. [2절 컬럼 보기](#7-2절-컬럼-보기)
8. [3절 데이터 조회](#8-3절-데이터-조회)
9. [4절 값으로 찾기](#9-4절-값으로-찾기)
10. [5절 값별 건수](#10-5절-값별-건수)
11. [6절 자유 SQL](#11-6절-자유-sql)
12. [7절 코드값 뜻 찾기](#12-7절-코드값-뜻-찾기)
13. [8절 결과 저장](#13-8절-결과-저장)
14. [결과의 의미와 보호장치](#14-결과의-의미와-보호장치)
15. [오류 해결](#15-오류-해결)
16. [종료·재실행·공유 체크리스트](#16-종료재실행공유-체크리스트)

## 1. 먼저 알아둘 것

- 이 도구는 **DB 조회용**입니다. ERP 화면의 수주 등록·수정·삭제 기능이 아닙니다.
- 대부분의 절은 코드를 수정하지 않고 입력창에 답하면 됩니다. 6절만 SQL을 직접 편집합니다.
- 가장 단순한 사용 순서는 **0 → 1 → 2 → 3 → 8**입니다. 모든 절을 순서대로 실행할 필요는 없습니다.
- **0절은 커널을 시작할 때 먼저 실행**하고, 필요한 조회 절만 개별 실행하세요.
- 결과는 `df`라는 변수에 보관됩니다. 1~7절 중 다른 셀을 실행하면 이전 결과가 교체됩니다.
- 결과를 저장하려면 원하는 조회 직후 8절을 실행하세요. 실패·취소한 조회의 이전 결과를 대신 저장하지 않도록 1~7절은 실행 시작 시 `df`를 비웁니다.
- 운영 DB에 접속하므로 작은 조회부터 시작합니다. 데이터가 많으면 기간과 컬럼을 줄이세요.

## 2. 설치와 커널 선택

### 2.1 준비물

- Python 3.10 이상과 프로젝트 가상환경 `.venv` (아래 설치 예시는 Python 3.13)
- 이 저장소의 파일들: 노트북, `hhhs_db_manager.py`, `pyproject.toml`
- 승인된 읽기 전용 DB 계정의 접속정보가 들어 있는 로컬 `.env`
- DB에 접근 가능한 네트워크 및 필요한 접근 승인
- VS Code의 Python/Jupyter 지원 또는 JupyterLab

노트북 하나만 다른 폴더에 복사하면 모듈을 찾지 못할 수 있습니다. 저장소 폴더에서 시작하는 방법을 권장합니다.

### 2.2 권장 설치: uv

터미널에서 **저장소 폴더로 이동한 후** 실행합니다. 프로젝트 안에서는 해당 폴더 이름이 `DB조회도구`일 수 있습니다.

```bash
# .venv가 없을 때만 생성합니다. 기존 가상환경을 덮어쓰지 마세요.
uv venv --python 3.13

# 노트북·Excel 저장에 필요한 선택 의존성까지 설치합니다.
uv pip install -e ".[notebook]"
```

`notebook` 의존성에는 `ipykernel`, `jupyterlab`, `openpyxl`이 포함됩니다. 마지막 것은 `.xlsx` 저장에 필요합니다.

uv를 사용하지 않는 macOS/Linux 환경이라면 다음처럼 설치할 수도 있습니다.

```bash
# .venv가 없는 경우에만 실행
python3 -m venv .venv
.venv/bin/python3 -m pip install -e ".[notebook]"
```

### 2.3 VS Code로 열기

1. 저장소 폴더를 VS Code에서 엽니다.
2. `hhhs_db_manager.ipynb`를 엽니다.
3. 우측 상단 **커널 선택 → Python 환경**에서 이 저장소의 `.venv`를 선택합니다.
4. 0절의 코드 셀을 실행합니다.

커널은 노트북 코드를 실행하는 Python 프로세스입니다. 다른 프로젝트의 Python을 선택하면 패키지를 설치했어도 `ModuleNotFoundError`가 발생할 수 있습니다.

선택한 환경을 확인하려면 별도 코드 셀에서 다음만 실행합니다. 환경변수 전체나 `.env` 내용은 출력하지 마세요.

```python
import sys
from pathlib import Path
print(sys.executable)
print(Path.cwd())
```

### 2.4 JupyterLab으로 열기

저장소 폴더의 터미널에서 실행합니다.

```bash
uv run jupyter lab hhhs_db_manager.ipynb
```

커널이 목록에 없다면 다음 명령으로 등록한 뒤 **Python (hhhs-db)**를 선택합니다.

```bash
uv run python3 -m ipykernel install --user --name hhhs-db --display-name "Python (hhhs-db)"
```

### 2.5 접속정보와 설정

기존 `.env`는 덮어쓰지 않습니다. 신규 환경의 접속정보 준비는 담당자 안내를 따르며, 비밀번호를 노트북 셀에 적지 않습니다.

접속정보 파일은 `HHHS_ENV_FILE` → 현재 작업 폴더의 `.env` → 모듈 폴더의 `.env` 순으로 찾습니다. 명시한 `HHHS_ENV_FILE` 경로가 없으면 다른 파일로 넘어가지 않고 실패합니다. 이미 설정된 `DB_*` 환경변수는 `.env`보다 우선합니다.

`HHHS_QUERY_TIMEOUT`, `HHHS_MAX_ROWS`, `HHHS_SCHEMA` 같은 **동작 설정은 모듈 import 전에 프로세스 환경변수로 설정**합니다. `.env`에 이 값을 적는 것만으로 적용된다고 생각하면 안 됩니다. 접속·설정 변경 후에는 커널을 재시작하세요. 일반 사용자는 기본 보호장치를 유지하는 것을 권장합니다.

## 3. 셀과 입력창 조작

| 조작 | 방법 |
| --- | --- |
| 셀 실행 | 코드 셀을 클릭하고 `Shift+Enter`, 또는 셀의 실행 버튼 |
| 입력값 제출 | 입력창에 값을 쓰고 Enter |
| 기본값 사용 | 입력하지 않고 Enter. `[100]`이면 100 사용 |
| 조건 없이 실행 | 기본값이 없는 조건 입력창을 비우고 Enter |
| 같은 조회 반복 | 같은 코드 셀을 다시 실행하고 조건 재입력 |
| 실행 중단 | 도구 모음의 중단/Interrupt 버튼. 드라이버 상태에 따라 즉시 끝나지 않을 수 있음 |
| 상태 초기화 | 커널 Restart 후 0절부터 재실행. 저장하지 않은 변수는 사라짐 |

- `모두 실행/Run All`은 여러 입력창과 운영 DB 조회를 연달아 실행하므로 권장하지 않습니다.
- 커널이 실행 중이면 다른 셀을 연속 클릭하지 말고 입력 대기 중인지 먼저 확인하세요.
- **앞 셀에서 입력한 값이 뒷 셀 기본값으로 이어집니다.** 테이블명은 2·3·4·5절이 공유하고, 컬럼명은 테이블별로, 최대 건수와 코드 필드명도 마지막 값을 기억합니다. 기억한 값은 `ask.memory` 에 들어 있고 커널을 재시작하면 지워집니다.
- 조건(WHERE)·정렬·컬럼 목록은 기억하지 않습니다. 비우면 '조건 없음'이라 이전 조건이 몰래 다시 걸리지 않습니다.
- 어느 경우든 **입력창 `[ ]` 안이 이번에 적용될 값**입니다. 그대로 쓰려면 비우고 Enter, 바꾸려면 새로 입력하세요.
- 4·5절에서 테이블만 이어받고 그 테이블의 컬럼을 아직 쓴 적이 없으면 컬럼을 물어봅니다. 2절에서 컬럼 목록을 먼저 확인하세요.
- 0절 오류 시에는 후속 셀이 일부 실행 가능해 보여도 진행하지 마세요. 점검 실패가 커널 전체를 자동 잠그는 것은 아닙니다.

## 4. 처음 한 번 따라 하기

다음은 **입력 연습 예시**이며, 조회 결과가 존재한다고 보장하지 않습니다.

1. **0절** 실행: 접속 완료와 결과 저장 폴더를 확인합니다.
2. **1절** 실행: 테이블 검색어에 `SA_SO`를 입력합니다.
3. **2절** 실행: 테이블명 `SA_SOH`, 컬럼 검색어는 비웁니다.
4. **3절** 실행: 아래 입력 예시를 사용합니다. 먼저 2절에서 컬럼이 있는지 확인하세요.

| 질문 | 입력 예시 |
| --- | --- |
| 테이블명 | `SA_SOH` |
| 컬럼 | `NO_SO, DT_SO, CD_PARTNER, STA_SO` |
| 조건 WHERE | `DT_SO >= '20260901' AND DT_SO < '20261001'` |
| 정렬 ORDER BY | `DT_SO DESC, NO_SO DESC` |
| 최대 건수 | `100` |

이 예시는 회사 필터가 없으므로 조회 가능한 여러 회사의 데이터가 포함될 수 있습니다. 실제 업무 조회에서는 **확인한 회사코드**로 조건을 추가하세요.

5. 결과의 컬럼·행수·기간이 목적과 맞는지 확인합니다.
6. 다른 조회 셀을 실행하지 말고 **8절**을 실행합니다.
7. `수주조회_202609.csv` 또는 `수주조회_202609.xlsx`를 입력합니다.
8. 출력된 저장 경로를 확인합니다. 같은 파일명이 있으면 다른 이름으로 저장합니다.

## 5. 0절 접속·권한 확인

조회 모듈과 도우미를 준비하고, 결과 저장 폴더를 설정한 뒤 접속 및 기본 스키마의 쓰기 권한을 점검합니다.

출력 문구 `쓰기 권한 점검 대상 테이블 수`의 값은 실제 코드상 **쓰기 권한이 발견된 테이블 수**입니다. 정상적인 사용 전제는 0이며, 0보다 크면 오류를 내고 담당자 확인을 요구합니다.

주의: 이 점검은 기본 스키마의 INSERT/UPDATE/DELETE 권한을 확인하는 보조 점검입니다. 모든 스키마·프로시저·DDL 권한을 포괄하는 보안 감사는 아닙니다. 최종 읽기 전용 보장은 서버 계정 권한에 의존합니다.

TLS 설정은 `hhhs_db_manager`를 DB 드라이버보다 먼저 import해야 합니다. 먼저 `pymssql` 등을 불러온 세션에서 TLS 오류가 나면 커널을 재시작하세요.

## 6. 1절 테이블 찾기

이름의 일부를 입력합니다. 예: `SO`, `QTIO`, `PRQ`, `ITEM`.

| 결과 열 | 의미 |
| --- | --- |
| `schema` | 스키마. 테이블명이 같으면 스키마까지 구분 |
| `table` | 조회에 사용할 테이블명 |
| `type` | 테이블 또는 뷰 |
| `rows` | 서버 카탈로그 행수. 정확한 COUNT 검산과는 다름 |
| `columns` | 컬럼 개수 |
| `modified` | 객체 수정일. 마지막 업무 데이터 입력일이라는 뜻은 아님 |

입력을 비우면 `min_rows=1`에 해당하는 목록을 행수 내림차순으로 표시합니다. **빈 테이블과 행수가 NULL인 일반 뷰는 숨겨질 수 있습니다.** 목록에 없다고 존재하지 않는다고 단정하지 마세요.

고급 사용자는 6절에 아래 코드를 대신 넣어 캐시를 새로 읽고 전체 목록을 볼 수 있습니다.

```python
df = None
df = show(db.get_list_tables(refresh=True), all_rows=True)
```

## 7. 2절 컬럼 보기

테이블명을 입력합니다. 스키마를 생략하면 기본 `NEOE`를 사용합니다. 다른 스키마는 `dbo.테이블명` 형태로 지정합니다.

컬럼 검색어는 영문 일부(`DT_`) 또는 한글(`일자`)로 입력하고, 전체를 보려면 비웁니다.

| 결과 열 | 의미 |
| --- | --- |
| `column` | 실제 SQL에 쓰는 영문 컬럼명 |
| `name_kr` | ERP 사전의 한글명. 없는 경우 빈 값 |
| `type` | 문자열·숫자 등 자료형과 길이 |
| `nullable` | NULL 허용 여부 |
| `pk` | 기본키 구성 순서. 없으면 빈 값 |

한글 사전을 읽지 못해도 기본 컬럼 조회가 가능하면 `name_kr`를 빈 값으로 유지합니다. **PK가 없다고 중복이 없다는 뜻은 아닙니다.** 조인·집계 시 논리 키와 중복 여부를 별도 확인하세요.

## 8. 3절 데이터 조회

입력값으로 `SELECT TOP 건수 컬럼 FROM 테이블 WHERE 조건 ORDER BY 정렬` 형태를 만듭니다.

- 컬럼: `NO_SO, DT_SO`처럼 **이름만 쉼표로 구분**. 수식·별칭은 6절에서 사용합니다.
- 조건: **WHERE 단어 없이** 조건만 입력합니다.
- 정렬: **ORDER BY 단어 없이** 컬럼과 `ASC`/`DESC`를 입력합니다.
- 건수: **1~10,000 정수**. 기본은 100입니다.
- 컬럼을 비우면 전체 컬럼을 가져옵니다. 넓은 테이블은 필요한 컬럼만 선택하세요.
- 정렬을 비우면 반환 순서는 보장되지 않습니다. 같은 조회를 반복해도 같은 순서를 원한다면 고유키까지 정렬에 포함합니다.

### 조건 작성 예시

각 조건은 해당 컬럼이 실제로 있는 테이블에서만 사용합니다.

| 목적 | 입력 예시 |
| --- | --- |
| 문자열 일자 범위 | `DT_SO >= '20260901' AND DT_SO < '20261001'` |
| 품목명 일부 | `NM_ITEM LIKE '%POLY%'` |
| 특정 상태 | `STA_SO = 'R'` — 코드 뜻은 7절 등으로 별도 확인 |
| 값 없음 | `CD_PARTNER IS NULL` — `= NULL`은 사용하지 않음 |
| 여러 조건 | `STA_SO = 'R' AND DT_SO >= '20260901'` |

텍스트는 작은따옴표로 감쌉니다. 문자열 일자와 진짜 날짜형 컬럼은 구분해야 합니다. 입력창에 적은 조건은 **SQL 원문**으로 처리하므로, 사용자가 만든 조건을 검토해야 합니다. 따옴표가 포함된 값이나 복잡한 조건은 6절의 매개변수 바인딩을 권장합니다.

3절의 TOP 100은 원래 결과가 100행을 넘는지 알려주지 않습니다. `truncated=False`여도 TOP 때문에 일부만 조회했을 수 있습니다.

## 9. 4절 값으로 찾기

품목명·거래처명 같은 문자열 컬럼에서 검색어가 포함된 행을 찾습니다.

| 질문 | 입력 예시 |
| --- | --- |
| 테이블명 | `MA_ITEM` |
| 찾을 컬럼 | `NM_ITEM` |
| 찾을 값 | `POLY` |
| 최대 건수 | `100` |

이 절은 검색값을 바인딩으로 전달하고 양쪽에 `%`를 붙여 부분일치 검색을 합니다. 검색값에 따옴표를 붙이지 마세요.

- `%`, `_`, 대괄호 패턴은 SQL Server LIKE의 패턴 의미를 가질 수 있습니다. 문자 자체의 정확한 일치 검색은 6절에서 `컬럼 = :value`를 사용하세요.
- 숫자·날짜의 범위 검색은 3절 또는 6절을 사용합니다.
- 이 절은 회사코드나 기간 입력창이 없습니다. 회사·기간 제한이 필요하면 3절 또는 6절로 전환하세요.
- 기본 정렬이 없으므로 ‘가장 최근 데이터’ 검색이 아닙니다.

## 10. 5절 값별 건수

상태·구분 코드가 각각 몇 건 있는지 분포를 확인합니다.

| 질문 | 입력 예시 |
| --- | --- |
| 테이블명 | `SA_SOH` |
| 집계할 컬럼 | `STA_SO` |
| 조건 WHERE | `DT_SO >= '20260901' AND DT_SO < '20261001'` |

결과는 `값`, `건수` 두 열이며 **건수 내림차순 상위 50개 그룹**입니다. 그룹이 50개보다 많다면 표시된 건수 합계가 전체 건수가 아닐 수 있습니다.

- 수주 HEAD의 건수와 LINE의 건수는 서로 다른 단위입니다.
- NULL도 하나의 그룹으로 나타날 수 있습니다.
- `COUNT(*)`는 롤 수·수량·중량의 합이 아니라 **행 개수**입니다.
- 집계 결과가 작아도 입력 테이블 전체를 읽을 수 있으므로 큰 테이블에는 기간 조건을 넣으세요.

## 11. 6절 자유 SQL

SQL을 직접 수정하는 절입니다. 기본 예시는 수주 HEAD와 거래처를 연결합니다. SQL의 `:co`, `:d`는 아래 Python 호출의 값으로 채웁니다.

```python
df = show(db.query(sql, co="1000", d="20260901"))
```

`1000`은 예시 값입니다. 조회할 회사를 확인한 후 변경합니다. 여러 조건값을 문자열 f-string으로 SQL에 붙이지 말고 매개변수로 넘기세요.

아래는 기존 예시를 단순화한 조회입니다. 0절 실행 후 6절의 코드를 바꿔 사용할 수 있습니다.

```python
df = None
sql = """
SELECT TOP 20 NO_SO, DT_SO, CD_PARTNER, STA_SO
FROM NEOE.SA_SOH
WHERE CD_COMPANY = :co
  AND DT_SO >= :start_date AND DT_SO < :end_date
ORDER BY DT_SO DESC, NO_SO DESC
"""
df = show(db.query(
    sql, co="1000", start_date="20260901", end_date="20261001"
))
```

- 위 코드는 사용 예시이며 이번 설명서 작성 중 운영 DB에 실행하지 않았습니다.
- 조인은 회사코드·필요한 공장코드 및 HEAD/LINE 키를 빠짐없이 확인합니다.
- 한 번에 한 SQL 문장을 실행합니다. 쓰기·실행·외부 접근 구문은 사용하지 않습니다.
- 서버 계정의 권한과 도구의 보조 검사를 우회하는 직접 엔진 사용은 권장하지 않습니다.
- 큰 결과를 얻으려고 바로 `max_rows=0` 또는 `allow_heavy=True`로 바꾸지 마세요. 먼저 기간·대상 컬럼을 줄이고 담당자와 범위를 협의합니다.

### 받은 결과를 추가 조회 없이 살펴보기

조회가 성공한 뒤 별도 코드 셀에서 사용할 수 있습니다.

```python
print(len(df))                       # 받은 행수
print(df.attrs.get("truncated"))     # 수신 상한에 의해 잘렸는지
print(df.columns.tolist())           # 컬럼 이름
print(df.dtypes)                     # pandas가 해석한 자료형
display(df.head(10))                 # 받은 결과 중 앞 10행만 표시
```

`df`가 `None`이면 먼저 조회해야 합니다. 위 명령은 새 SQL을 실행하지 않습니다.

## 12. 7절 코드값 뜻 찾기

입력창에 `수주상태`, `작업상태`, `수불구분` 같은 **코드 필드 한글명 일부**를 넣습니다.

결과 열은 `CD_FIELD`, `필드`, `값`, `뜻`입니다. 현재 셀은 `MA_CODE`와 `MA_CODEDTL`을 연결하며, **공통 회사 `MASTER`와 사용 여부 `Y`로 제한**합니다.

- 입력창에 `STA_SO` 같은 컬럼명을 넣으면 필드 한글명 검색이므로 결과가 없을 수 있습니다.
- 회사별 코드·비활성 코드까지 모두 보여주는 기능은 아닙니다.
- 같은 코드값이 여러 필드에서 다른 뜻으로 쓰일 수 있습니다. 값이 `R`이라고 해서 항상 ‘확정’으로 해석하지 마세요.
- 컬럼과 코드 필드의 정확한 대응은 실제 값 집합·화면·담당자 확인이 필요합니다.

## 13. 8절 결과 저장

원하는 결과가 나온 **직후** 실행합니다. 파일명만 입력하세요.

```text
수주조회_202609.csv
```

또는:

```text
수주조회_202609.xlsx
```

### 무엇을 저장하는가

가장 최근 조회 결과 `df` 전체를 저장합니다. 화면에서 생략된 행도 `df`에 있으면 저장하지만, TOP 또는 수신 상한 때문에 **애초에 받지 않은 행은 저장하지 못합니다.**

| 마지막 실행한 절 | 저장 대상 |
| --- | --- |
| 1절 | 테이블 목록 |
| 2절 | 컬럼 목록 |
| 3·4·6절 | 데이터 조회 결과 |
| 5절 | 값별 건수 표 |
| 7절 | 코드 설명 표 |

### 저장 위치와 실패 처리

- 저장 위치는 **0절에서 출력한 `결과 저장 폴더`**입니다. 노트북 경로가 아니라 현재 작업 폴더를 기준으로 정합니다.
- 현재 작업 폴더 이름이 `DB조회도구`이면 그 상위 프로젝트 폴더에 저장합니다. 별도 clone인 `hhhs-db-manager`에서 실행하면 해당 현재 폴더에 저장합니다.
- 업무 자료를 프로젝트 밖에 저장하지 않도록 0절 출력 경로를 확인한 뒤 조회·저장을 진행하세요.
- `/폴더/파일.csv`, `../파일.csv` 같은 경로 입력은 거부합니다.
- 기존 파일과 심볼릭 링크에는 저장하지 않습니다. `FileExistsError`이면 다른 파일명으로 다시 실행하세요.
- CSV는 `utf-8-sig`로 저장합니다. Excel에서 CSV를 바로 열면 앞자리 0이 있는 코드나 날짜가 자동 변환될 수 있으므로 **텍스트/CSV 가져오기에서 해당 열을 텍스트로 지정**하세요.
- 실패한 Excel 저장은 빈 파일 또는 불완전 파일을 남길 수 있습니다. 성공 메시지가 없었다면 파일 존재만으로 성공이라 판단하지 말고 원인을 해결한 뒤 새 이름으로 저장하세요.
- CSV·Excel을 열 때 외부 콘텐츠·수식 실행을 허용하지 마세요. DB 문자열을 일반 텍스트로 다뤄야 하며, 현재 저장 셀은 수식처럼 해석될 수 있는 문자열을 별도로 무해화하지 않습니다.

## 14. 결과의 의미와 보호장치

### 세 가지 행수 제한을 구분하세요

| 구분 | 동작 | 주의 |
| --- | --- | --- |
| SQL TOP | 3·4절 입력 건수 등으로 DB 결과 개수 제한 | 더 많은 행이 원래 있는지는 별도 확인 필요 |
| 수신 상한 | 기본 10,000행. 넘으면 경고와 `df.attrs['truncated']=True` | 저장해도 상한 밖 행은 없음 |
| 화면 표시 | 일반 결과는 pandas 표시 설정으로 일부 행·열 생략 가능 | `…`만으로 수신 잘림을 판단하지 않음 |

- 기본 제한시간은 쿼리 60초입니다. 대량 집계·조인·정렬은 TOP이 있어도 느릴 수 있습니다.
- 큰 테이블 보호는 SQL 형태를 보는 보조 장치입니다. WHERE나 집계가 있다는 이유만으로 가벼운 쿼리라는 뜻은 아닙니다.
- 프로세스당 동시 실행 1개와 느린 조회 후 휴지가 적용됩니다. 노트북 여러 개를 각각 열면 전체 서버 부하가 늘 수 있습니다.
- `READ UNCOMMITTED`는 데이터 잠금 경합을 줄이지만, 커밋 전 데이터·누락·중복이 섞일 수 있습니다. 스키마 잠금 등 모든 잠금을 없애지도 않습니다.
- 날짜를 하루 전으로 제한해도 일관성이 자동 보장되지는 않습니다. 마감·확정 수치는 승인된 일관성 있는 환경에서 검산합니다.
- 결과를 보고 업무 시스템에 자동 쓰기하거나 상태 코드를 임의 해석하지 않습니다.

## 15. 오류 해결

| 증상 | 먼저 할 일 |
| --- | --- |
| `ModuleNotFoundError: hhhs_db_manager` | 저장소 폴더에서 설치했는지, 해당 `.venv` 커널인지 확인 |
| `ModuleNotFoundError: openpyxl` | `uv pip install -e ".[notebook]"` 후 올바른 커널로 재실행 |
| `NameError: ask/show/db` | 커널이 바뀌었거나 0절 미실행. 0절부터 실행 |
| 입력창이 안 보이고 실행 중 | 입력 대기 UI 확인. 셀을 중복 실행하지 말고 필요하면 중단 |
| `ConfigError` | 접속정보 담당자와 파일 위치·계정 유효성 확인. 내용을 출력하지 않음 |
| `HHHS_ENV_FILE` 파일 없음 | 명시한 경로 수정. 다른 파일로 자동 대체되지 않음 |
| TLS 협상 실패 | 커널 재시작 후 0절 먼저 실행 |
| 서버에 닿지 못함 | 네트워크·접근 허용 여부를 담당자에게 확인. 접속주소를 공유 로그에 남기지 않음 |
| `PermissionDenied` | 대상 테이블 SELECT 권한을 담당자에게 확인 |
| `QueryRejected` | 잘못된 이름·건수·다중 문장 또는 보호장치. 우회하지 말고 조건 수정 |
| 테이블/컬럼 없음 | 1·2절에서 실제 이름과 스키마 확인 |
| `QueryTimeout` | 기간·조인·컬럼 범위를 축소. 반복 재시도로 밀어붙이지 않음 |
| 결과가 0행 | 회사코드·일자 형식·NULL·검색어·상태 조건을 하나씩 점검 |
| `상한에서 잘림` | 기간을 나누거나 조건을 좁힘. 전체 결과라고 보고하지 않음 |
| `FileExistsError` | 기존 파일은 보존하고 새 이름 사용 |
| 저장할 결과 없음 | 1~7절 중 원하는 조회가 성공한 후 8절 실행 |
| 쓰기 권한 발견 | 0절에서 멈추고 담당자에게 확인. 후속 셀 실행 금지 |

문제 전달 시에는 사용한 절 번호, 오류 종류, 수행 시각, 민감값을 제거한 조건 형태만 공유하세요. `.env`, 전체 traceback, 조회 원본을 외부 서비스에 붙여 넣지 않습니다.

## 16. 종료·재실행·공유 체크리스트

### 매번 조회할 때

- [ ] 올바른 프로젝트 `.venv`와 작업 폴더인가?
- [ ] 0절이 정상 종료했는가?
- [ ] 테이블·컬럼·회사·기간을 확인했는가?
- [ ] 작은 건수와 필요한 컬럼부터 조회했는가?
- [ ] 결과가 HEAD 건수인지 LINE 건수인지, 단위가 무엇인지 확인했는가?
- [ ] TOP·화면 생략·수신 상한을 구분했는가?
- [ ] 원하는 조회 직후 저장하고 실제 저장 경로·성공 메시지를 확인했는가?

### 종료와 공유

- 파일을 저장한 뒤 노트북의 **모든 출력 지우기/Clear All Outputs**를 실행하고 노트북을 저장합니다.
- 출력뿐 아니라 직접 수정한 SQL·매개변수·Markdown에도 실제 거래처·주문·접속정보가 없는지 확인합니다.
- 필요가 끝나면 커널을 종료합니다. 브라우저 탭을 닫는 것만으로 커널이 종료되는 것은 아닙니다.
- 다음 세션에서는 0절부터 다시 실행합니다. 이전 변수는 재사용할 수 없습니다.
- 조회 CSV·Excel과 내부 자료는 외부 저장소에 추가하지 않습니다. 저장 위치가 저장소 상위 폴더라면 이 저장소의 `.gitignore` 보호를 받지 않는다는 점도 유의하세요.

### 검증 범위

이 설명서는 해당 커밋의 노트북 코드와 README를 대조해 작성했습니다. 조회 코드의 오프라인 회귀 테스트와 노트북 코드 구문·출력 제거 상태는 확인했지만, 예시 SQL의 운영 DB 결과, 모든 UI 환경의 대화형 실행, 실제 Excel 파일 저장 성공을 보장하는 문서는 아닙니다.
